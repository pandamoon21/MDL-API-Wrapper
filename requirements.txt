# Pinned to versions verified in CI/local (2026-09-02). For deploys & Docker.
# Library consumers: pyproject.toml uses >= bounds so pip can resolve freely.
fastapi==0.141.1
uvicorn[standard]==0.52.4
httpx==0.28.1
curl_cffi==0.16.2

# Optional DB cache backends (install only if you use them):
#   MySQL:      pip install pymysql
#   PostgreSQL: pip install "psycopg[binary]"
