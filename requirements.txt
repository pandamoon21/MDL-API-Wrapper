fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27

# Optional: curl_cffi transport (browser/mobile TLS impersonation for
# Cloudflare-challenged IPs). Install only if you set MDL_TRANSPORT=curl_cffi:
#   pip install curl_cffi

# Optional DB cache backends (install only if you use them):
#   MySQL:      pip install pymysql
#   PostgreSQL: pip install "psycopg[binary]"
